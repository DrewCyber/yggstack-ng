# smoltcp compile-time config: set reassembly buffer to 65536 bytes so that
# fragmented IPv6 packets carrying large TLS payloads can be reassembled.
[env]
SMOLTCP_REASSEMBLY_BUFFER_SIZE = "65536"
SMOLTCP_REASSEMBLY_BUFFER_COUNT = "4"
